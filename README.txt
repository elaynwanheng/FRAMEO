FRAMEO PHOTO BOOTH — PREVIOUS RATIO / NO MIRROR

This version keeps the previous camera-frame sizing/layout.
Only the mirror operation has been removed.

- Front/user-facing camera
- Live preview NOT mirrored
- Captured photo NOT mirrored
- Previous camera-frame proportion/layout retained
- 3-second countdown
- One photo per session
- A7 receipt
- Device timestamp
- Grayscale/high-contrast photo
- frameo. logo
- RETAKE / PRINT
- Home Screen standalone manifest
- Hidden 5-tap settings lock (PIN 2026 for development)

For iPad kiosk use: Add the website to Home Screen, then use Guided Access.
Safari/iPadOS controls true browser fullscreen behavior.

PRINT currently uses the system/browser print flow. Direct silent ESC/POS
printing to an Xprinter XP-80T will need a native/bridge printing layer.
